import React from 'react';
import './Main.scss';

const Main = () => {
	return (
		<main className="main">
			<h1>MAIN MENU</h1>
			<button>Select Character</button>
			<button>Options</button>
			<button>Sign Out</button>
		</main>
	);
};

export default Main;
